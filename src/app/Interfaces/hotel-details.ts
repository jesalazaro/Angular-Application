export interface HotelDetails {
    id: number;
    direccion: string; // Nueva propiedad para dirección
    tipo_propiedad: string; // Nueva propiedad para tipo de propiedad
    numero_habitaciones: number; // Nueva propiedad para número de habitaciones
    numero_banos: number; // Nueva propiedad para número de baños
    numero_cuenta: string; // Nueva propiedad para número de cuenta
    id_usuario: number; // Nueva propiedad para ID de usuario
}
